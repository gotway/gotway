name: Build

on: [push, pull_request]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: 1.15

      - name: Check out code
        uses: actions/checkout@v2

      - name: Build
        run: make build

      - name: Build Catalog
        working-directory: ./microservice/catalog
        run: make build

      - name: Build Route
        working-directory: ./microservice/route
        run: make build

      - name: Build Stock
        working-directory: ./microservice/stock
        run: make build

      - name: Bin
        uses: actions/upload-artifact@v2
        with:
          name: gotway
          path: ./bin/gotway

      - name: Bin Catalog
        uses: actions/upload-artifact@v2
        with:
          name: catalog
          path: ./microservice/catalog/bin/catalog

      - name: Bin Route
        uses: actions/upload-artifact@v2
        with:
          name: catalog
          path: ./microservice/route/bin/route

      - name: Bin Stock
        uses: actions/upload-artifact@v2
        with:
          name: stock
          path: ./microservice/stock/bin/stock
